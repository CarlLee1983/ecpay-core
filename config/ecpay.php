<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 綠界 ECPay SDK 共用設定
    |--------------------------------------------------------------------------
    |
    | 這是所有 ECPay 子套件的共用基礎設定。
    | 各子套件會有自己的設定檔來覆寫或擴充這些設定。
    |
    */

    /*
    |--------------------------------------------------------------------------
    | 預設環境
    |--------------------------------------------------------------------------
    |
    | 設定預設使用的環境。可選值：'production' 或 'sandbox'
    |
    */
    'environment' => env('ECPAY_ENVIRONMENT', 'sandbox'),

    /*
    |--------------------------------------------------------------------------
    | SSL 驗證
    |--------------------------------------------------------------------------
    |
    | 是否啟用 SSL 憑證驗證。正式環境強烈建議啟用。
    |
    */
    'verify_ssl' => env('ECPAY_VERIFY_SSL', true),

    /*
    |--------------------------------------------------------------------------
    | HTTP 請求設定
    |--------------------------------------------------------------------------
    */
    'http' => [
        // 請求逾時時間（秒）
        'timeout' => env('ECPAY_HTTP_TIMEOUT', 30),

        // 連線逾時時間（秒）
        'connect_timeout' => env('ECPAY_HTTP_CONNECT_TIMEOUT', 10),
    ],
];

